To run this project after cloning the repository and adding your own API keys:

- Make sure you have pipenv installed (i.e. pipenv --version)

- To activate the virtual environment run the command: pipenv shell

- To start the bot run the command: python main.py